/*O comando If Else é uma estrutura de controle que da a possibilidade 
com base em uma condição qual fluxo que a aplicação irá tomar.
É atravez dele que podemos fazer diversas verificações para criar
fluxos dentro das nossas aplicações que atendam a todos os tipos
de possibilidades, ou todas aquelas que sejam possiveis de serem
previstas.*/

/* Existem diversas aplicações que não controlam determinados fluxos,
e isso acontece por N motivos. Nesses casos, nos termos potenciais bugs.
Portanto, quanto mais controlarmos os fluxos dentro de nossas aplicações
mais confiaveis elas serão.*/


/** Sintaxe do comando:
if(condição) {
    //trecho de código que será executado
    
} else if (condição) {
    //trecho de c[odigo que será executado
    
} else {
    //trecho de codigo que será executado
    
    
}
**/


/***********  Operadores de comparação (condicionais)*************/

/*Também conhecidos como operadores relacioonais */

/*Servem para formar as expressões condicionais para o comando if*/

// Igual (==) - Verifica se os valores comparados são *iguais*
// Idêntico (===) - Verifica se os valores comparados são *iguais e do mesmo tipo*
// Diferente (!=) - Verifica se os valores comparados são *diferentes*
// Não idêntico (!===) - Verifica se os valores comparados são *diferentes e de tipos diferentes*
// Menor (<) - Verifica se o valor da esquerda é *menor* que o valor da direita
// Maior (>) - Verifica se o valor da esquerda é *maior* que o valor da direita
// Menor igual (<=) - Verifica se o valor da esquerda é *menor ou igual* que o valor da direita
// Maior ou igual (>=) - Verifica se o valor da esquerda é *maior ou igual* que o valor da direita
