/****** 
Solução If Else
*******/








/****** 
Solução Ternário
*******/

var idade = prompt('Digite sua idade:')

var kid = (idade >= 0 && idade < 15) ? 'criança' : jovem
var jovem = (idade >=15 && idade < 30) ? 'jovem' : adulto  
var adulto = (idade >= 30 && idade < 60)