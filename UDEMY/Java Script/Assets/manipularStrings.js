/* Valores primitivos como strings por si só não possuem propiedades ou  metodos.
Porem quando eles são interpretados pelo JS, o JS tem a inteligência de convertelos
em objetos e com isso agregar propiedades e metodos pre estabelecidos da linguagem.*/ 

/*Antes de explorarmos atributos e metodos de objetos strings, um detalhe importante:
cada caractere contido dentro de uma string ocupa uma posição sequêncial especifica,
sendo que essa posição sequeêncial inicia em 0. */

/*É muito comum trabalharmos sobre dados do tipo string, a fim de customizar de alguma forma
os textos que serão exibidos pro usuario, ou mesmo a forma como esses textos serão
armazenados no banco de dados. E ai, para nos ajudar nas principais tratativas de strings
do dia a dia a propia linguagem JS, disponibilza uma serie de atributos e metodos nativos, e apartir
deles nos podemos resolver diversas situações. */

/*Uma dica bacana para ter acesso  a relação de propiedades e atributos de objetos string
em JS, é consultar esses recursos atravez de*/

